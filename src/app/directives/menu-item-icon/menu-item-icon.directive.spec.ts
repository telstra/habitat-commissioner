import { MenuItemIconDirective } from './menu-item-icon.directive';

describe('MenuItemIconDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuItemIconDirective();
    expect(directive).toBeTruthy();
  });
});
